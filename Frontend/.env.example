VITE_BASE_URL=http://localhost:8000
VITE_JIKAN_API_URL=https://api.jikan.moe/v4

# Notes:
# - Vite requires env vars exposed to client to start with `VITE_`.
# - Adjust `VITE_BASE_URL` to your backend server URL in production.
