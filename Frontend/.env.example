# Local development
VITE_BASE_URL=http://localhost:8000

# Production (Render backend URL — no trailing slash)
# VITE_BASE_URL=https://your-backend.onrender.com

# Notes:
# - Vite requires env vars exposed to client to start with `VITE_`.
# - Set VITE_BASE_URL to your Render backend URL in production.
